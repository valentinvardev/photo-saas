/* Public demo route for the Brooklyn delivery template.
   The component itself lives in ./component so it can be imported by the
   editor and the public /d/[id] route without violating Next.js's rule
   that page files only allow a default export. */
export { default } from "./component";
