/**
 * CanvasFrame is now a thin wrapper that injects palette CSS variables
 * onto .canvas-frame. Device framing and viewport-aware rendering live
 * in Canvas.tsx. CanvasFrame is no longer used directly — kept for
 * future use if needed.
 */
export {};
